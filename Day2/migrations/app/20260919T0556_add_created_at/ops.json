[
  {
    "id": "column.public.tickets.created_at",
    "label": "Add column \"created_at\" to \"tickets\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "created_at",
        "table": "tickets"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"created_at\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "tickets",
          "created_at"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"created_at\"",
        "sql": "ALTER TABLE \"public\".\"tickets\" ADD COLUMN \"created_at\" timestamptz DEFAULT (now()) NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"created_at\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "tickets",
          "created_at"
        ]
      }
    ]
  }
]